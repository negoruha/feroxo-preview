# Feroxo — Contact fixes (28 June 2026)

Updated only the Contact page to match the approved Figma reference:

- Replaced the four unicode contact symbols with the supplied Figma SVG assets.
- Preserved the approved map SVG at its exact 908×416 proportion and positioned the second red marker lower on the map.
- Replaced the footer social sprite with the supplied SVG and made the four social links render correctly.
- Removed the duplicate rendered tagline under the footer logo; the logo SVG already contains the tagline.
- Applied the Desktop - 5 footer geometry and spacing, with mobile fallback retained.

## Updating the existing GitHub Pages preview

Use the PowerShell commands supplied in the chat to copy this archive into `C:\FeroxoPreview`, commit, push, then hard-refresh the preview after GitHub Pages rebuilds.
