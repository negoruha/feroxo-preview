# About Us — three supplied reference blocks (02.07)

Changed only the three requested blocks on `pages/about.html`:

1. Proven in Practice / 170+ (exact icon size, copy scale, positioning and dark red overlay)
2. Cost-saving Reliability (exact supplied icon exports, type scale, spacing and overlay)
3. 1973 timeline card (exact card proportion, generator treatment, typography and column ratio)

The text remains semantic DOM content with existing `data-i18n` keys, so both English and Czech continue to translate instead of being baked into a static image.
