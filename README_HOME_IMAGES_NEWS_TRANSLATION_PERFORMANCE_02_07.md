# Home image-only hero, Latest News red interaction and image delivery — 02.07

Only the requested Home/translation/performance work was applied:

1. The Home hero carousel is image-only. The hero `<video>` element and every hero video trigger were removed. The separate Built for Industry local video module remains unchanged.
2. All four Latest News & Articles tiles have one consistent Feroxo red hover/focus sweep and a lightweight image zoom.
3. The Home’s heavy hero secondary photos and news/module imagery now use optimized WebP assets. Non-initial hero slides are loaded just before use and warmed after the first paint. Below-fold home images use native lazy loading and async decoding.
4. English/Czech controls and the shared translation script were validated on all 28 public site pages. The Home language layer was aligned with the approved Make Request English copy.

No other visual page content was deliberately changed.
